syntax = "proto3";

/*
    Task - create a message Latitude Longitude (example: -33.865382, 151.192861). With fields:
    Latitude (number)
    Longitude (number)
*/

message LatLong {
    double latitude = 1; // double vs float - more precision than float
    double longitude = 2;
}