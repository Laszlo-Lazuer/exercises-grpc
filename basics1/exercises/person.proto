syntax = "proto3";

/*
    Task - define a message for     Person (example: Michael Jordan):
        - First Name
        - List of middle names
        - Last Name
*/

message Person {
    string first_name = 1;
    repeated string list_of_middle_names = 2;
    string last_name = 3;
}